---
version: 1
handlers:
  hidden:
    class: logging.handlers.MemoryHandler
    capacity: 1024
  memory:
    class: logging.handlers.MemoryHandler
    capacity: 1024 * 1024
    target: console
    level: SPAM
  console:
    class: logging.StreamHandler
    level: SPAM
    stream: ext://sys.stdout
loggers:
  gitflow_wotw:
    level: DEBUG
    handlers: [console]
    propagate: no
root:
  level: WARNING
  handlers: [hidden]
